name: claudewerk-staging

services:
  broker:
    build: .
    container_name: broker-staging
    restart: unless-stopped
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - DEEPGRAM_API_KEY=${DEEPGRAM_API_KEY:-}
    ports:
      - "${PORT:-19999}:9999"
    volumes:
      - broker-staging-data:/data/cache
      - ./web/dist:/srv/web:ro
    command:
      - "--web-dir"
      - "/srv/web"
      - "--cache-dir"
      - "/data/cache"
      - "--rp-id"
      - "localhost"
      - "--origin"
      - "http://localhost:19999"
      - "--rclaude-secret"
      - "${RCLAUDE_SECRET}"
      - "--verbose"

volumes:
  broker-staging-data:
